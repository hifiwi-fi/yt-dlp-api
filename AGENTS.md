# Agent Guidelines

Write Markdown prose with one sentence per source line.
Use one newline between sentences that remain in the same paragraph.
Use two newlines, represented by a blank line, to start a new paragraph.
